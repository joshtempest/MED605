using UnityEngine;
using UnityEngine.XR.Interaction.Toolkit.Locomotion.Teleportation;

public class Teleport_fade_black_Ray : TeleportationProvider

{
    // find the comaands for teleporting ak metods and put it in if x OR y is executed then ply fde
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
